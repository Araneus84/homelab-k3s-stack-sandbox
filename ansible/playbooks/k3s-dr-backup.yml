---
# Install/update k3s DR backup timer on the homelab host (run from your laptop).
# NFS must be mounted on that host. See docs/disaster-recovery.md
#   ansible-playbook playbooks/k3s-dr-backup.yml

- name: Configure k3s DR backup to NAS
  hosts: k3s_servers
  become: true
  gather_facts: true

  roles:
    - role: k3s_dr_backup
      tags: [k3s_dr_backup, dr]
