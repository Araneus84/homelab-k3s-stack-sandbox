# {{ ansible_managed }}
# fail2ban jail configuration for homelab

[DEFAULT]
bantime = 3600
findtime = 600
maxretry = 5
backend = systemd
banaction = ufw

[sshd]
enabled = true
port = {{ ssh_port }}
filter = sshd
maxretry = {{ ssh_max_auth_tries }}
bantime = 7200
findtime = 600
