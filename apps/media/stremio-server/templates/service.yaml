apiVersion: v1
kind: Service
metadata:
  name: {{ include "stremio-server.fullname" . }}
  labels:
    {{- include "stremio-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "stremio-server.selectorLabels" . | nindent 4 }}
