apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "autobrr.fullname" . }}
  labels:
    {{- include "autobrr.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  strategy:
    type: {{ .Values.strategy.type }}
  selector:
    matchLabels:
      {{- include "autobrr.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "autobrr.selectorLabels" . | nindent 8 }}
    spec:
      {{- if .Values.securityContext }}
      securityContext:
        {{- toYaml .Values.securityContext | nindent 8 }}
      {{- end }}
      initContainers:
        - name: init-config-permissions
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              set -eu
              chown -R 1000:1000 /config
              chmod -R u+rwX,g+rwX /config
          volumeMounts:
            - name: config
              mountPath: /config
          # Pod-level securityContext sets runAsNonRoot=true; explicitly allow root for the init step.
          securityContext:
            runAsUser: 0
            runAsGroup: 0
            runAsNonRoot: false
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: TZ
              value: {{ .Values.env.TZ | quote }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: config
              mountPath: /config
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: {{ include "autobrr.fullname" . }}-config
