# Prowlarr indexer manager configuration
replicaCount: 1

image:
  repository: lscr.io/linuxserver/prowlarr
  tag: latest
  pullPolicy: Always

# Environment
env:
  PUID: "1000"
  PGID: "1000"
  TZ: "Asia/Jerusalem"

service:
  type: ClusterIP
  port: 9696

homepageDiscovery:
  enabled: true
  name: Prowlarr
  group: Media
  icon: si-prowlarr

ingress:
  enabled: true
  className: nginx
  host: prowlarr.home
  annotations: {}

# Persistent storage for Prowlarr config
persistence:
  config:
    enabled: true
    storageClass: local-path-homelab
    size: 2Gi
    accessMode: ReadWriteOnce

# NFS: NAS export path (server filesystem) vs mountPath (inside container; match qBittorrent)
nfsMedia:
  enabled: true
  server: "192.168.50.110"
  path: /mnt/HD/HD_a2/NAS
  mountPath: /mnt/nas

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi

nodeSelector: {}
tolerations: []
